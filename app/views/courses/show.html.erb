<%= render 'layouts/menu' %>
<section>
  <div class="row">
    <div class="rec rec-big">
      <article>
        <h1><%= @course.Name %></h1>
        <p>
          <%= @course.body %>
          <% if @course.tags.any? %>
            <ul>
              <% @course.tags.each do |tag| %>
                  <li><%= tag.name %></li>
              <% end %>
            </ul>
        <% end %>
        </p>
      </article>
    </div>
  </div>
</section>
