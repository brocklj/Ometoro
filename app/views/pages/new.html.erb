<%= form_for :page, url: pages_path do |f| %>
    <p>
      <%= f.label :title %><br>
      <%= f.text_field :title %>
    </p>

    <p>
      <%= f.label :test %><br>
      <%= f.text_area :test %>
    </p>

    <p>
      <%= f.submit %>
    </p>
<% end %>

<%= link_to 'back', pages_path %>