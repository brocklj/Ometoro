<%= simple_form_for([:admin,@page, @course]) do |f| %>

    <p>
      <%= f.label :Name %><br>
      <%= f.text_field :Name %>
    </p>
    <p>
      <%= f.label :body %><br>
      <%= f.text_area :body %>

    <div class="field"></div>
      <%= f.association :page %>
    </p>
      <%= f.association :tags, as: :check_boxes %>
    <%= link_to 'Add Tag', new_admin_tag_path(page_id:@page, course_id:@course) %>




    <p>
      <%= f.submit %>
    </p>
<% end %>